rootProject.name = 'reactor'

include 'reactor-core',
		'reactor-bus',
		'reactor-alloc',
		'reactor-stream',
		'reactor-logback',
		'docs'

if(System.env['User'] != "Bamboo"){
	include 'reactor-pylon'
}
