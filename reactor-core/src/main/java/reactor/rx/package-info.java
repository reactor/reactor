/**
 * Streams are reactive components that provide a composition API for reacting to {@link reactor.rx.Signal}
 * rather than by using {@link reactor.event.selector.Selector Selectors}
 * and {@link reactor.core.Reactor Reactors} directly.
 */
package reactor.rx;