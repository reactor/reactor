package reactor.logging;

import org.slf4j.LoggerFactory;

public class ReactorLoggerFactory implements IReactorLoggerFactory {
    @Override
    public ReactorLogger getLogger(String name) {
        return null;
    }
}
